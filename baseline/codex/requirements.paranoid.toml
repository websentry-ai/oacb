# OACB Codex Requirements — paranoid tier (enterprise enforcement)
#
# Deploy to /etc/codex/config.toml via MDM to lock policy settings.
#
# _oacb:
# name:    "OACB Codex Requirements"
# version: "0.1.1"
# tier:    "paranoid"

[requirements]
approval_policy = "untrusted"
sandbox_mode    = "read-only"
